{
  "name": "hnwhoishiring-bench",
  "private": true,
  "type": "module",
  "description": "Reproducible startup + render benchmark for the app (Playwright/Chromium, recorded fixtures, synthetic network).",
  "scripts": {
    "record": "node run.mjs --record",
    "bench": "node run.mjs",
    "compare": "node compare.mjs",
    "smoke": "node smoke.mjs",
    "stale-mix": "node stale-mix.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
